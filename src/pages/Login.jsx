import React, { useContext } from 'react'
import { UserContext } from '../context/UserContext'

function Login() {
  return (
    <div>
        <h1>Login</h1>
        <button>Login</button>
    </div>
  )
}

export default Login